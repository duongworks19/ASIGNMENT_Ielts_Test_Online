# CONTEXT.md — Feature: Shared UI & API Infrastructure

Feature này thuộc phạm vi của thành viên khác trong nhóm (hoặc cả nhóm cùng đóng góp).
Hiện tại chỉ tạo placeholder để giữ cấu trúc SDD thống nhất.
Không triển khai chi tiết trong task Guest/Public Homepage.

## Phạm vi dự kiến
- Axios instance cấu hình chung.
- Shared components: LoadingSpinner, EmptyState, ErrorBoundary, Pagination.
- Layout wrappers: MainLayout, StudentLayout, TeacherLayout, AdminLayout.
- Theme/CSS variables theo DESIGN.md.
- Toast notification setup (react-hot-toast).
- Route guards / Protected routes.

> **TODO:** Member phụ trách sẽ bổ sung sau.
